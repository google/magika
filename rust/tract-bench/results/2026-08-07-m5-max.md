# Apple M5 Max feasibility result

Date: 2026-08-07

## Environment

- Magika base commit: `94ffd1d`
- Host: Apple M5 Max, 18 cores, 128 GB memory
- OS: macOS 26.5.2 (25F84), arm64
- Rust: rustc 1.95.0, cargo 1.95.0
- ONNX Runtime: `ort 2.0.0-rc.12`
- tract: exact version `0.23.4`
- Build: release, one codegen unit, LTO, stripped
- Input: batch 1, features extracted from `README.md`

The CPU comparison below is the median of six 200-iteration trials. Backend order alternated
between trials with `--reverse`. Times are microseconds. Cold time is model load plus runtime
preparation; first is the first inference after preparation; the remaining columns follow ten
unreported warmups.

## Correctness

| Comparison | Maximum absolute score difference | Winning labels |
| --- | ---: | --- |
| ONNX Runtime vs tract CPU | 2.4883775e-9 | identical |
| tract CPU vs tract Metal | 2.3841858e-7 | identical |

The harness checks every row and fails if a winning label differs or any score differs by more
than `1e-4`.

## Codec-equivalent model footprint

| Representation | ONNX bytes | Dynamic NNEF bytes | NNEF change |
| --- | ---: | ---: | ---: |
| Raw model/container | 3,163,737 | 3,165,696 | +0.0619% |
| gzip level 9 | 2,919,550 | 2,917,395 | -0.0738% |
| zstd level 19 | 2,918,205 | 2,916,168 | -0.0698% |

The earlier comparison between raw ONNX and gzip-compressed NNEF incorrectly attributed
compression savings to the format. With identical codecs, model size is a wash. The checked NNEF
artifact is gzip-compressed; `scripts/measure-size.sh` reproduces all three rows.

## Executable footprint

| Artifact | Bytes | Change from ORT |
| --- | ---: | ---: |
| ONNX Runtime-only benchmark executable | 19,776,896 | baseline |
| Initial single-thread tract CPU-only executable | 15,989,376 | -19.15% |
| Optimized two-thread tract CPU-only executable | 16,900,704 | -14.54% |
| tract CPU + Metal benchmark executable | 18,028,032 | -8.84% |
| Current dynamic owner-capable ORT-only executable | 19,876,400 | baseline |
| Current dynamic owner-capable tract CPU-only executable | 16,967,072 | -14.64% |
| Current dynamic owner-capable tract CPU + Metal executable | 18,939,024 | -4.72% |

Each executable embeds its model. The runtime-specific harness code and release profile are the
same. `otool -L` shows no separately shipped ONNX Runtime dylib in this configuration. The current
Metal build adds 11.62% over tract CPU and links the system Metal framework. Historical and
current rows are kept separate because the harness gained dynamic conversion and owner-pool code.

## CPU timing

| Backend | Cold | First | Mean | p50 | p95 |
| --- | ---: | ---: | ---: | ---: | ---: |
| ONNX Runtime | 13,151 | 2,598 | 2,339 | 2,353 | 2,688 |
| tract CPU | 21,317 | 3,179 | 2,883 | 2,823 | 3,551 |
| tract change | +62.10% | +22.36% | +23.28% | +19.98% | +32.13% |

tract CPU lands almost exactly on the spike's 20% warm-p50 limit, but misses it on mean and p95.
Its 19.15% footprint reduction also misses the initial 30% target.

## CPU optimization follow-up

The follow-up removes three avoidable costs:

- the harness now spawns one tract execution state and reuses it like a long-lived Magika session;
- tract's optional Rayon matrix-multiplication executor is enabled, with two threads as the default
  after end-to-end sweeps of one, two, and four threads;
- conversion replaces the `[1, 2048, 257]` one-hot tensor and its first matrix multiplication with
  an equivalent 257-row embedding gather.

The gather cuts the profiled embedding path from roughly 0.39 ms (equality, cast, packing, and
matmul) to roughly 0.03 ms. It preserves the original maximum score difference of
`2.4883775e-9` and identical winning labels. The converter emits the rewrite deterministically.

The table below is the median of ten alternating-order 1,000-iteration release trials. The harness
was also corrected so `First` is the actual first inference after preparation; the initial table's
`First` values inadvertently measured the second inference after parity verification.

| Backend | Cold | First | Mean | p50 | p95 |
| --- | ---: | ---: | ---: | ---: | ---: |
| ONNX Runtime | 11,001 | 2,317 | 1,763 | 1,724 | 2,064 |
| optimized tract CPU | 18,373 | 2,048 | 1,750 | 1,661 | 2,133 |
| tract change | +67.02% | -11.61% | -0.74% | -3.63% | +3.32% |

The optimized tract CPU path is effectively tied on warm mean, 3.63% faster at p50, and 3.32%
slower at p95. A separate interleaved thread-count sweep confirmed two threads beat one end to end
(median p50 1,717 us versus 1,858 us). Rayon costs 911,328 bytes versus the initial single-thread
tract executable, leaving the optimized CPU executable 14.54% smaller than the ORT executable.

## Accumulated CPU batch timing

The converter now preserves a symbolic batch dimension, and one prepared tract state successfully
runs changing batch sizes `1, 2, 3, 8, 16` with identical winning labels. The largest observed ORT
score difference is `2.3841858e-7`.

The table is the median of three alternating-order 200-iteration release sweeps with four tract
matrix-multiplication threads. Inputs are accumulated into contiguous `[batch, 2048]` tensors before
the timer. Per-file mean is batch mean divided by batch size; throughput is files per second.

| Batch | ORT per file (us) | ORT files/s | tract per file (us) | tract files/s | tract throughput change |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 1,702 | 587 | 2,126 | 470 | -19.9% |
| 2 | 1,536 | 651 | 2,168 | 461 | -29.1% |
| 4 | 1,501 | 666 | 2,150 | 465 | -30.2% |
| 8 | 1,430 | 699 | 2,175 | 460 | -34.3% |
| 16 | 1,408 | 710 | 2,219 | 451 | -36.5% |
| 32 | 1,357 | 737 | 2,204 | 454 | -38.4% |

Four threads improve tract's larger-batch throughput over the batch-one-optimal two-thread setting,
but tract remains nearly flat while ORT amortizes work across the batch. Eight threads produced the
best isolated batch-32 tract sample (465 files/s), still about 36% behind its paired ORT result
(730 files/s); 16 and 18 threads regressed. A single global four-thread setting is therefore not a
CPU fix, and the earlier two-thread batch-one result should not be generalized to CLI throughput.

## Dedicated compute-owner timing

The new execution prototype does not reuse the ORT async-session architecture. One dispatcher feeds
bounded queues; dedicated OS threads own non-`Send` states and return completion messages. The model
is prepared once as a `Send + Sync` runnable shared by all owners, while each owner spawns and retains
its own private mutable state.

An initial batch-8 sweep held the nominal compute budget to eight threads:

| Topology | ORT files/s | tract files/s |
| --- | ---: | ---: |
| 1 owner x 8 threads | 685 | 453 |
| 2 owners x 4 threads | 1,209 | 797 |
| 4 owners x 2 threads | 1,252 | 1,391 |
| 8 owners x 1 thread | 2,057 | 2,110 |

The exact crossover is noisy under sustained load. Across three alternating-order measurements, the
median 4 x 2 result is 1,422 files/s for ORT and 1,347 for tract (-5.2%); the median 8 x 1 result is
2,025 for ORT and 1,879 for tract (-7.2%). Twelve and sixteen single-thread tract owners regress in
the initial sweep, to 1,983 and 1,814 files/s. The important result is architectural: inter-batch
owner concurrency closes most of the tract gap, while adding threads inside one state does not.

Isolated `/usr/bin/time -l` runs show the RAM cost. A tract-only process uses 75.0 MB maximum RSS
with one owner and 405.8 MB with eight. Sharing one prepared runnable reduces the eight-owner peak
from 449.2 MB, but mutable state storage still dominates. Comparable ORT-only runs use 91.3 MB and
540.7 MB, so tract's eight-owner peak is about 25% lower, though neither is a lightweight default.

## Metal timing

This is the median of five 200-iteration trials in a tract CPU + Metal executable. CPU ran before
Metal in each trial, so treat small differences as directional rather than conclusive.

| Backend | Cold | First | Mean | p50 | p95 |
| --- | ---: | ---: | ---: | ---: | ---: |
| tract CPU | 19,979 | 2,611 | 2,394 | 2,314 | 2,736 |
| tract Metal | 73,768 | 5,192 | 2,271 | 2,172 | 2,905 |
| Metal change | +269.23% | +98.85% | -5.14% | -6.14% | +6.18% |

Metal does not meet the 15% warm improvement threshold for batch 1. It also adds 2,038,656 bytes
to the tract CPU executable and greatly increases cold preparation.

The dynamic model enables a directional 100-iteration batch sweep. Metal is slower than ORT through
batch 4, then leads by about 3.7% at batch 8, 4.0% at batch 16, 9.3% at batch 32, 13.8% at batch 64,
and 16.9% at batch 128. Only the artificial batch-128 case clears the 15% threshold. Metal model
preparation is roughly 71-84 ms in these runs, and a new shape can add substantial first-run work;
the batch-2 first inference reached 93 ms. Metal score differences remain within tolerance (maximum
`1.5199184e-5`) with identical labels.

## Conversion and integration blockers

- The deterministic NNEF archive loads with `tract-core` extensions only; it does not need the
  ONNX parser or ONNX-only tract-OPL registry at runtime.
- Conversion now rewrites the embedding while the graph is symbolic, replaces exporter-generated
  dynamic shape inputs with equivalent `TDim` constants, and removes two full-range slices whose
  sentinel end value tract otherwise propagates as a literal dimension. The checked artifact has
  input shape `[N, 2048]`.
- tract's state is mutable and not `Send`. The tract path must use the new bounded
  accumulator/compute-owner design, not reuse the ORT `num_tasks`/async-session architecture.
  Dedicated owner threads retain private states spawned from one shared prepared runnable;
  synchronous CPU work stays off Tokio.
- Owner count needs a platform- and memory-aware default. Eight owners are fastest on this host but
  consume about 406 MB peak RSS; 12 and 16 owners regress.
- `tract-metal` pulls `tract-gpu`, `tract-transformers`, and `tract-pulse-opl`, which explains much
  of the optional backend's footprint cost.

## Recommendation

Do not integrate tract into the production library yet. Dynamic conversion and parity are solved,
but codec-equivalent model size is a wash and the deployable CPU harness is only 14.54% smaller.
The batch-one-tuned CPU result is competitive, while representative batches in one state are 29-38%
behind ORT. The new dedicated-owner topology narrows that to roughly 5-7% at four or eight owners,
within the original runtime threshold, but its RAM cost needs an explicit budget and sustained-load
results remain variable. Continue this architecture as a prototype before production integration;
do not reuse the ORT Tokio execution model. Metal remains unjustified for normal batches and should
stay out of the default build.
