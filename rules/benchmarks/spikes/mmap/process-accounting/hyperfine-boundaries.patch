diff --git a/src/timer/mod.rs b/src/timer/mod.rs
index b2cd265..e855795 100644
--- a/src/timer/mod.rs
+++ b/src/timer/mod.rs
@@ -92,6 +92,8 @@ pub fn execute_and_measure(mut command: Command) -> Result<TimerResult> {
         command.creation_flags(CREATE_SUSPENDED);
     }
 
+    let trace_boundary = std::env::var_os("MAGIKA_PARENT_TRACE").is_some();
+    let start_unix_ns = std::time::SystemTime::now().duration_since(std::time::UNIX_EPOCH).unwrap().as_nanos();
     let wallclock_timer = WallClockTimer::start();
     let mut child = command.spawn()?;
 
@@ -109,7 +111,11 @@ pub fn execute_and_measure(mut command: Command) -> Result<TimerResult> {
     let status = child.wait()?;
 
     let time_real = wallclock_timer.stop();
+    let end_unix_ns = std::time::SystemTime::now().duration_since(std::time::UNIX_EPOCH).unwrap().as_nanos();
     let (time_user, time_system, memory_usage_byte) = cpu_timer.stop();
+    if trace_boundary {
+        eprintln!("{{\"hyperfine_boundary\":1,\"start_unix_ns\":{start_unix_ns},\"end_unix_ns\":{end_unix_ns},\"elapsed_seconds\":{time_real}}}");
+    }
 
     Ok(TimerResult {
         time_real,
